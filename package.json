{
  "name": "rabwah-factory",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd rabwah-factory-backend && nodemon src/server.js",
    "client": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.10"
  }
}
